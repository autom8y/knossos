# Minimal team orchestrator for fallback testing
team:
  name: minimal-team
  domain: testing
  color: gray

frontmatter:
  role: "Minimal testing team"
  description: |
    A minimal team for testing fallback context generation.

routing:
  solo-agent: "Handles all work for minimal team"

workflow_position:
  upstream: "Test input"
  downstream: "Test output"
