---
description: Quick switch to 10x-dev (full development workflow)
argument-hint: [--force]
allowed-tools: Bash, Read
model: haiku
---

## Context
Auto-injected by SessionStart hook (project, rite, session, git).

## Your Task

Switch to the 10x development rite and display the pantheon. $ARGUMENTS

## Behavior

1. Execute: `ari sync materialize --rite 10x-dev $ARGUMENTS`
2. Display the roster output from ari (agents and their roles)
3. If SESSION_CONTEXT exists, update `active_rite` to `10x-dev`

## When to Use

- Starting feature development
- Full PRD -> TDD -> Code -> QA workflows
- Any work requiring the 4-agent development lifecycle

## Reference

Full documentation: `.claude/skills/10x-ref/skill.md`
