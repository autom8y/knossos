# {domain}.just
# {Description of what this module handles}
#
# Recipes:
#   {domain}           - Meta-task (runs all)
#   {domain}:primary   - Primary operation
#   {domain}:secondary - Secondary operation

# === Meta-task ===
# Default: run all tasks in this domain
{domain}: {domain}:primary {domain}:secondary
    @just _log "{domain} complete"

# === Primary Tasks ===

# {Description of primary task}
{domain}:primary:
    @just _log "Running {domain}:primary..."
    # TODO: Add implementation
    @just _log "{domain}:primary complete"

# {Description of secondary task}
{domain}:secondary:
    @just _log "Running {domain}:secondary..."
    # TODO: Add implementation
    @just _log "{domain}:secondary complete"

# === Helpers (if needed) ===

# [private]
# _{domain}_helper:
#     # Internal helper for this domain
