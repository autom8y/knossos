---
summary: Session Or State -- the directory that stores all session lifecycle data.
see_also: [session, knossos]
aliases: []
---
Session Or State -- the `.sos/` directory that stores all session state including active sessions, the archive, lock files, and CC session maps. SOS is the operational state layer that tracks what work is in progress.
