# Bug Triage Log - 2026-01-04

## Critical Sync Issues

### 1. Incomplete Swap Recovery Failure
**Status**: CRITICAL
**Trigger**: `roster-sync sync --refresh` after incomplete swap
**Symptoms**:
- Incomplete swap detected from sre → ecosystem in COMMITTING phase
- `swap-rite.sh --auto-recover` succeeds but doesn't fully reset state
- Subsequent sync attempts show new incomplete swap (sre → sre in PREPARING phase)

**Root Cause**: Backup validation failing after rollback; swap journal corrupted

**Impact**: Users stuck in unrecoverable state without manual intervention

---

### 2. Orphan Agent Handling in Non-Interactive Mode
**Status**: HIGH
**Trigger**: `roster-sync sync --refresh` with ecosystem agents in project
**Symptoms**:
- roster-sync detects 5 orphan agents: compatibility-tester, context-architect, documentation-engineer, ecosystem-analyst, integration-engineer
- Tool fails with exit code 5: "Orphan agents detected in non-interactive mode"
- Requires manual flag (--keep-all, --remove-all, or --promote-all)

**Root Cause**: roster-sync doesn't auto-handle orphans from previous team swaps

**Impact**: Blocks sync operations, forces user intervention

---

### 3. COMMAND_REGISTRY.md Conflict During Sync
**Status**: MEDIUM
**Trigger**: `roster-sync sync --refresh`
**Symptoms**:
- Warning: "Conflict: COMMAND_REGISTRY.md (local modified, roster updated)"
- Requires --force flag to proceed

**Root Cause**: Unknown; git status shows clean working tree but roster-sync detects conflict

**Impact**: Confusing UX, blocks sync without --force

---

### 4. Corrupted Swap Journal After Rollback
**Status**: HIGH
**Trigger**: `swap-rite.sh --auto-recover` followed by `roster-sync sync --refresh`
**Symptoms**:
- `.swap-journal` created with PREPARING phase
- Backup location `.swap-backup/` missing or corrupted
- sync fails: "Backup is missing or corrupted"

**Root Cause**: Auto-recover creates new swap journal without completing it

**Impact**: Requires manual cleanup (rm .swap-journal) before next sync attempt

---

## Affected Components

- `swap-rite.sh` - Auto-recovery creates incomplete state
- `roster-sync` - Doesn't handle orphans or conflicts in non-interactive mode
- `.swap-journal` - Backup paths corrupted/missing after rollback

## Recommended Actions

1. **Immediate**: Enhance `swap-rite.sh --auto-recover` to fully reset `.swap-journal`
2. **High Priority**: Make roster-sync auto-handle orphans with sensible defaults (promote cross-pack agents)
3. **Medium Priority**: Investigate COMMAND_REGISTRY.md conflict source and auto-resolution

## Next Session

Schedule deep triage session with hygiene to debug and fix all 4 issues.
